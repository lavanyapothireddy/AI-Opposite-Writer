services:
  - type: web
    name: ai-opposite-writer
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server-static.js
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: GROQ_API_KEY
        sync: false
